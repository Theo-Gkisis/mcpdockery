[project]
name = "mcpdockery"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "docker>=7.1.0",
    "mcp[cli]>=1.28.1",
]
